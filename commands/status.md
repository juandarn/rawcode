---
description: Show project status — git state, recent changes, file summary
---

Show the current project status as a concise dashboard:

1. Run `git status --short` for working tree state
2. Run `git log --oneline -10` for recent commits
3. Run `git diff --stat` for changed files summary

Format everything in under 20 lines. No explanations, just the data.
